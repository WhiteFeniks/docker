# 09. Из оболочки контейнера debian установите через менеджер пакетов контейнера 
# все необходимое для компиляции исходного кода C и поместите его в репозиторий git
# (конечно, перед этим убедитесь, что менеджер пакетов и пакеты, уже находящиеся в
# контейнере, обновлены). В этом упражнении вы должны указать только те команды,
# которые будут выполняться непосредственно в контейнере

# Запускаем контейнер Debian:
# docker run -ti --rm debian
# Выполняем в контейнере:

apt-get update
apt-get upgrade -y
apt-get install -y build-essential git

# build-essential содержит список пакетов, необходимых для создания пакетов,
# компилятор gcc, make и т.п.

# Для получения последней стабильной версии выпуска Debian
# apt-get install git



