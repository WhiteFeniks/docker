# 08.Запустите оболочку из alpine контейнера и убедитесь, что вы можете 
# взаимодействовать непосредственно с контейнером через терминал и что 
# контейнер удаляется после выполнения оболочки

docker run -it --rm alpine /bin/sh

# Additionally
# -i — Держите STDIN открытым, даже если он не подключен.
# -t — Выделите pseudo-TTY (Устройство которое имеет функции физического терминала, 
# фактически не будучи им)
# --rm — Автоматически извлекайте контейнер, когда он выходит

# Образ Alpine не содержит bash, поэтому используем /bin/sh
